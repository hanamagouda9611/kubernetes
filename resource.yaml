apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
  namespace: <namespace>
spec:
  hard:
    pods: "20"  # Increase the limit
    requests.cpu: "10"  # Adjust based on available CPU
    requests.memory: "20Gi"  # Adjust based on available RAM
